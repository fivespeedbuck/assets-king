# 资产大王 Agent 规则

## 项目记忆

- 主会话开始需求、缺陷、设计、测试或发布任务前，先按 `E:\codex\obsidian\00-入口\启动记忆.md` 与 `E:\codex\obsidian\00-入口\读取路由.md` 读取资产大王当前资料。
- 当前状态、需求、门禁与发布事实以 `E:\codex\obsidian\02-项目\资产大王` 为准；历史命令不能覆盖当前状态文档的结论。

## 分工

- 主会话负责产品与账务判断、架构、跨模块代码、Room 迁移和最终验收。
- 测试、Lint、AndroidTest、APK 构建、签名与实体校验交给轻量代理；默认使用用户指定的 GPT-5.6 Luna / max，除非用户另有要求。
- 机械任务只回传摘要、失败点和产物身份，不把完整 Gradle 日志灌入主会话。

## 轻量代理上下文

- 主会话负责先读取项目记忆并把任务目标、相关文件、禁止项、执行命令和验收标准压缩成可独立执行的任务说明。
- 审查、测试、构建和发布等轻量代理默认不重复执行 Obsidian 启动记忆流程，不扫描项目笔记、归档或历史会话；只读取任务说明点名的文件以及完成任务直接必需的源码或脚本。
- 只有任务明确要求独立产品判断、任务说明缺少会改变结果的关键事实，或用户明确要求时，轻量代理才按指定范围补读 Obsidian；不得自行扩展为全库扫描。
- 轻量代理遇到缺少上下文时，优先向主会话回报具体缺失事实，由主会话补充最小信息，不自行加载无关历史。

## Android 验证与交付

1. 按风险选择最小验证路径。先确认选中的测试是否真的覆盖改动；`--tests` 只筛测试用例，仍可能编译整个 `app` 测试变体，不能把它误称为单文件编译。
2. 日常验证复用 Gradle daemon、Kotlin 增量编译和本地缓存；不执行 `clean`，不使用 `--no-daemon`、`-Pkotlin.incremental=false` 或 `--max-workers=1`，除非正在做有明确对照组的性能/隔离取证。
3. 已通过且源码未再影响其覆盖范围的测试不重复执行。失败后先修编译或测试阻塞，再重跑受影响的最小集合。
4. 普通 UI/低风险改动采用两阶段交付：最小编译与定向验证通过后生成一个签名 Release 快速包供用户验收；用户确认同一份源码后，再执行一次完整门禁并复用该源码产出的正式 APK。
5. Room 迁移、数据丢失、安全、签名和核心账务改动在快速包前必须完成与风险相称的定向测试；发布前仍需完整门禁。
6. 构建或门禁异常缓慢时使用 `android-gradle-performance` skill 取证，报告具体任务耗时、executed / up-to-date / from-cache 统计和未执行项；不要凭 APK 大小或 Compose 印象判断。

## UI 硬性验收

- UI 不是“功能可用”即可交付的软指标。文字、标签、输入框、按钮、卡片和列表列必须在目标设备截图/真机上核对对齐、层级、间距、尺寸、行高、文字基线以及状态表达。
- 同类信息必须使用稳定的布局列或卡片分区；流水等列表的隐形列必须固定宽度，时间/金额按明确的对齐规则展示，不得因正负号或金额位数造成视觉跳动。
- 不接受字段堆叠、无意义大空白、异常换行、文字截断、芯片裁切、按钮错位或信息层级混乱。用户指出这些问题时，必须重做对应布局并重新复核，不能以“逻辑已实现”作为完成理由。
- 周期账单、确认流水、创建流水、贷款等表单优先按信息职责拆成清晰独立卡片，复用 `GlassCard` 与现有 UI 令牌；禁止把不同职责的字段、说明、选项和操作无边界堆在同一层级。

## 工作树与状态

- 保留既有未提交改动；不得 reset、clean、覆盖或顺手提交无关文件，不使用 `git add .`。
- 未经用户明确确认，不提交、不推送、不发布。
- 每次明确区分：源码已修改、已编译/验证、已构建 APK、已安装、已真机通过、已提交/推送、已发布。
