package com.assetsking.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──
val BrandGreen = Color(0xFF42D0A3)

// ── Light theme glass surfaces ──
val GlassCardLight = Color.White.copy(alpha = 0.72f)
val GlassBorderLight = Color.White.copy(alpha = 0.24f)
val SurfaceLight = Color(0xFFF2F2F7) // iOS-style cool gray background

// ── Dark theme glass surfaces ──
val GlassCardDark = Color.Black.copy(alpha = 0.48f)
val GlassBorderDark = Color.White.copy(alpha = 0.08f)
val SurfaceDark = Color(0xFF1C1C1E) // iOS-style dark background

// ── Semantic ──
val IncomeGreen = Color(0xFF34C759)
val ExpenseRed = Color(0xFFFF3B30)
val TextPrimaryLight = Color(0xFF1D1D1F)
val TextSecondaryLight = Color(0xFF8E8E93)
val TextPrimaryDark = Color(0xFFF5F5F7)
val TextSecondaryDark = Color(0xFF98989D)
